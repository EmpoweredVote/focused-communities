---
phase: 05-frontend-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/index.html
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.app.json
  - frontend/tsconfig.node.json
  - frontend/public/_redirects
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/index.css
  - frontend/src/vite-env.d.ts
  - frontend/src/lib/apiFetch.ts
  - frontend/src/context/AuthContext.tsx
  - frontend/src/components/AuthGate.tsx
  - frontend/src/types.ts
  - src/routes/communities.ts
autonomous: true

must_haves:
  truths:
    - "The frontend app starts with `npm run dev` and renders a page at http://localhost:5173"
    - "Navigating to /communities shows a placeholder DirectoryPage component"
    - "Navigating to /communities/:slug shows a placeholder CommunityHubPage component"
    - "Navigating to /communities/:slug/threads/:id shows a placeholder ThreadPage component"
    - "Navigating to an unknown route shows a NotFoundPage"
    - "GET /api/communities/by-slug/:slug returns the community object for a valid slug"
    - "Auth state resolves without errors (loading -> inform or connected)"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vite config with React plugin, Tailwind plugin, and /api proxy"
    - path: "frontend/src/main.tsx"
      provides: "Provider stack: QueryClientProvider > BrowserRouter > AuthProvider > ErrorBoundary > App"
    - path: "frontend/src/App.tsx"
      provides: "Route tree with all 4 routes + Toaster mount"
    - path: "frontend/src/types.ts"
      provides: "Shared TypeScript interfaces for all API response types"
    - path: "frontend/src/lib/apiFetch.ts"
      provides: "Migrated apiFetch with Vite env var for API base URL"
    - path: "frontend/src/context/AuthContext.tsx"
      provides: "Migrated AuthProvider with token lifecycle"
    - path: "frontend/src/components/AuthGate.tsx"
      provides: "Migrated AuthGate and SuspendedNotice"
    - path: "src/routes/communities.ts"
      provides: "New GET /api/communities/by-slug/:slug route"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/App.tsx"
      via: "Provider stack wrapping App component"
      pattern: "<QueryClientProvider.*<BrowserRouter.*<AuthProvider.*<App"
    - from: "frontend/src/App.tsx"
      to: "react-router"
      via: "Routes/Route declarations"
      pattern: "<Route path=.*/communities"
    - from: "frontend/vite.config.ts"
      to: "backend localhost:3000"
      via: "dev server proxy for /api"
      pattern: "proxy.*'/api'.*localhost:3000"
---

<objective>
Scaffold the standalone React/Vite frontend app, install all dependencies, configure Tailwind v4, set up the provider stack (QueryClient, BrowserRouter, AuthProvider, ErrorBoundary), define the route tree, migrate the three existing frontend-src primitives, create shared TypeScript types for all API responses, add the missing GET /api/communities/by-slug/:slug backend route, and wire placeholder page components for all routes.

Purpose: Every subsequent plan depends on a working frontend app with routing, providers, and types. This plan creates the foundation that Plans 02-05 build on.
Output: A running Vite dev server at localhost:5173 with working routing, auth context, and API proxy to backend.
</objective>

<execution_context>
@C:\Users\Chris\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Chris\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-ui/05-CONTEXT.md
@.planning/phases/05-frontend-ui/05-RESEARCH.md
@frontend-src/lib/apiFetch.ts
@frontend-src/context/AuthContext.tsx
@frontend-src/components/AuthGate.tsx
@src/routes/communities.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend slug route + Vite app scaffold + dependency install</name>
  <files>
    src/routes/communities.ts
    frontend/package.json
    frontend/index.html
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.app.json
    frontend/tsconfig.node.json
    frontend/public/_redirects
    frontend/src/vite-env.d.ts
    frontend/src/index.css
  </files>
  <action>
    **Step 1 — Backend slug route:**
    Add `GET /api/communities/by-slug/:slug` to `src/routes/communities.ts` immediately after the existing `GET /api/communities/:id` route. It is nearly identical to the `:id` route but uses `.eq('slug', req.params.slug)` instead of `.eq('id', req.params.id)`. Apply the same `cacheMiddleware(TTL.COMMUNITIES)` and return the same response shape `{ data: Community }`. Return 404 with code `COMMUNITY_NOT_FOUND` when not found.

    **Step 2 — Scaffold Vite app:**
    Run `npm create vite@latest frontend -- --template react-ts` from the project root. This creates `frontend/` with the default Vite React TypeScript template.

    **Step 3 — Install dependencies:**
    From `frontend/`, run:
    ```
    npm install react-router @tanstack/react-query tailwindcss @tailwindcss/vite sonner react-loading-skeleton react-error-boundary
    ```

    **Step 4 — Configure Vite:**
    Replace `frontend/vite.config.ts` with:
    - `react()` plugin from `@vitejs/plugin-react`
    - `tailwindcss()` plugin from `@tailwindcss/vite`
    - `server.proxy`: `{ '/api': 'http://localhost:3000' }`

    **Step 5 — Configure Tailwind v4:**
    Replace `frontend/src/index.css` contents with just `@import "tailwindcss";` — no tailwind.config.js needed for v4.

    **Step 6 — SPA routing fallback:**
    Create `frontend/public/_redirects` with content: `/* /index.html 200`

    Remove the default Vite template files that are not needed: `frontend/src/App.css`, `frontend/src/assets/` directory. Keep `frontend/src/vite-env.d.ts`.
  </action>
  <verify>
    Run `cd frontend && npm run dev` — the Vite dev server starts without errors at localhost:5173.
    Run `curl http://localhost:3000/api/communities/by-slug/climate` — returns `{ data: { id: ..., slug: "climate", ... } }` (or whatever test slug exists).
  </verify>
  <done>Vite app scaffolded, all dependencies installed, Tailwind v4 configured via Vite plugin, dev proxy active, backend slug route returns community data.</done>
</task>

<task type="auto">
  <name>Task 2: Provider stack, route tree, migrated primitives, shared types, placeholder pages</name>
  <files>
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/types.ts
    frontend/src/lib/apiFetch.ts
    frontend/src/context/AuthContext.tsx
    frontend/src/components/AuthGate.tsx
    frontend/src/pages/DirectoryPage.tsx
    frontend/src/pages/CommunityHubPage.tsx
    frontend/src/pages/ThreadPage.tsx
    frontend/src/pages/NotFoundPage.tsx
    frontend/src/components/AppCrashFallback.tsx
  </files>
  <action>
    **Step 1 — Shared types (frontend/src/types.ts):**
    Create TypeScript interfaces matching every API response shape. These are the canonical frontend types — all hooks and components import from here.

    ```typescript
    export interface Community {
      id: string
      slug: string
      name: string
      description: string
      memberCount: number
      threadCount: number
      topicId: string
      sliceLabel: string | null
      createdAt: string
    }

    export interface Stance {
      position: number       // INTERNAL ONLY — never render this value
      text: string           // headline
      supportingPoints: string[]
      description: string
      examplePerspectives: string[]
    }

    export interface ThreadSummary {
      id: string
      title: string
      excerpt: string
      authorPseudonym: string
      replyCount: number
      createdAt: string
      lastActivityAt: string
    }

    export interface Thread {
      id: string
      title: string
      body: string
      authorPseudonym: string
      replyCount: number
      createdAt: string
      lastActivityAt: string
      updatedAt: string
      isEdited: boolean
    }

    export interface Post {
      id: string
      authorPseudonym: string
      body: string
      createdAt: string
      updatedAt: string
      isEdited: boolean
    }

    export interface ThreadEdit {
      id: string
      oldTitle: string | null
      oldBody: string | null
      editedAt: string
    }

    export interface PostEdit {
      id: string
      oldBody: string
      editedAt: string
    }

    export interface PaginatedResponse<T> {
      data: T[]
      meta: { cursor: string | null; hasMore: boolean }
    }
    ```

    Add a TSDoc comment on the `Stance.position` field: `/** INTERNAL — never render to users. Display order is randomized. */`

    **Step 2 — Migrate apiFetch:**
    Copy `frontend-src/lib/apiFetch.ts` to `frontend/src/lib/apiFetch.ts`. The file uses `import.meta.env.VITE_API_BASE_URL` which is native Vite syntax — clean up the `as unknown` cast to use the proper Vite type: `import.meta.env.VITE_API_BASE_URL ?? ''`. In dev mode, the proxy handles `/api` — so the default should be empty string (not `http://localhost:3000`), letting relative URLs go through the Vite proxy.

    **Step 3 — Migrate AuthContext:**
    Copy `frontend-src/context/AuthContext.tsx` to `frontend/src/context/AuthContext.tsx`. Same cleanup — use `import.meta.env.VITE_ACCOUNTS_API_URL` with proper Vite types. Default to `'https://accounts.empowered.vote'`.

    **Step 4 — Migrate AuthGate:**
    Copy `frontend-src/components/AuthGate.tsx` to `frontend/src/components/AuthGate.tsx`. Update the import path to `'../context/AuthContext'`.

    **Step 5 — AppCrashFallback component:**
    Create `frontend/src/components/AppCrashFallback.tsx`:
    - Full-page centered error display
    - Heading: "Something went wrong"
    - Body: brief message
    - Button: "Reload" that calls `window.location.reload()`
    - Style with Tailwind: centered flex column, generous padding, muted colors from CivicSpace design system (`text-gray-600`, white background)

    **Step 6 — Placeholder page components:**
    Create minimal placeholder components for each page. Each should render a simple heading and receive route params (where applicable) so the routing can be verified:

    - `DirectoryPage.tsx`: renders `<h1>Communities</h1>` with a `<p>Directory page — coming in plan 05-02</p>`
    - `CommunityHubPage.tsx`: reads `slug` from `useParams()`, renders `<h1>{slug}</h1>` placeholder
    - `ThreadPage.tsx`: reads `slug` and `id` from `useParams()`, renders placeholder with both params
    - `NotFoundPage.tsx`: renders "Page not found" with a link back to `/communities`

    **Step 7 — main.tsx (provider stack):**
    Wire the full provider stack per RESEARCH.md Pattern 2:
    ```
    QueryClientProvider (staleTime: 30_000, retry: 1)
      > BrowserRouter
        > AuthProvider
          > ErrorBoundary (FallbackComponent={AppCrashFallback})
            > App
    ```
    Import `react-loading-skeleton/dist/skeleton.css` here for global skeleton styles.

    **Step 8 — App.tsx (route tree + Toaster):**
    Wire routes per RESEARCH.md Pattern 3:
    - `"/"` -> `Navigate to="/communities" replace`
    - `"/communities"` -> `DirectoryPage`
    - `"/communities/:slug"` -> `CommunityHubPage`
    - `"/communities/:slug/threads/:id"` -> `ThreadPage`
    - `"*"` -> `NotFoundPage`

    Mount `<Toaster position="bottom-center" richColors />` from sonner above the Routes.

    Remove the default Vite template `App.tsx` content entirely — no counter, no Vite/React logos.
  </action>
  <verify>
    Run `cd frontend && npm run build` — TypeScript compiles without errors.
    Open http://localhost:5173/ — redirects to /communities and shows "Communities" heading.
    Navigate to /communities/test-slug — shows "test-slug" heading.
    Navigate to /communities/test-slug/threads/some-id — shows both params.
    Navigate to /foo — shows "Page not found" with link to /communities.
  </verify>
  <done>
    Provider stack renders without errors. All 4 routes resolve to correct placeholder pages. Shared types exist for all API contracts. Three migrated primitives (apiFetch, AuthContext, AuthGate) are integrated. Toaster is mounted. Error boundary catches unhandled errors.
  </done>
</task>

</tasks>

<verification>
1. `cd frontend && npm run dev` starts without errors
2. `cd frontend && npm run build` compiles without TypeScript errors
3. `curl http://localhost:3000/api/communities/by-slug/climate` returns valid community JSON
4. Browser navigation to all 4 routes renders correct placeholder content
5. `frontend/src/types.ts` contains Community, Stance, ThreadSummary, Thread, Post, ThreadEdit, PostEdit, PaginatedResponse interfaces
6. No `tailwind.config.js` file exists — Tailwind v4 uses Vite plugin only
7. `frontend/public/_redirects` contains SPA fallback rule
</verification>

<success_criteria>
- Vite dev server runs at localhost:5173 with HMR
- All routes render correct placeholder pages
- Backend slug route is live and cached
- TypeScript builds clean — no type errors
- Provider stack (QueryClient + BrowserRouter + Auth + ErrorBoundary + Toaster) is wired
- apiFetch, AuthContext, and AuthGate are migrated and importable
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-ui/05-01-SUMMARY.md`
</output>
